import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

// Proxy only applies in development (npm run dev)
// Production build uses config.js BACKEND_URL directly — no proxy needed
export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:5000',
        changeOrigin: true,
      },
      '/socket.io': {
        target: 'http://localhost:5000',
        changeOrigin: true,
        ws: true,
      },
    },
  },
  build: {
    outDir: 'dist',
    sourcemap: false,
  },
});
